class Solution:
    def isPalindrome(self, x: int) -> bool:
        number = x
        str_number = str(number)

        rev = str_number[::-1]

        if str_number == rev:
            return True
        else:
            return False
