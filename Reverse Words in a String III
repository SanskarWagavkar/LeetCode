class Solution:
    def reverseWords(self, s: str) -> str:
        list_str = s.split(" ")
        rev_list = []
        for i in list_str:  
            rev_list.append(i[::-1])
        
        word = " ".join(rev_list)
        return word
